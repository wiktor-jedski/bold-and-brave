{
  "name": "bold-and-brave",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck:core": "tsc -b tsconfig.core.json",
    "typecheck:browser": "tsc -b tsconfig.browser.json",
    "test:simulation": "vitest run src/core/simulation",
    "test:runtime": "vitest run src/browser/runtime",
    "test:support": "vitest run src/browser/support",
    "test:startup": "vitest run src/browser/startup",
    "test:presentation": "vitest run src/browser/presentation",
    "test:support-row": "vitest run scripts/check-support-row.test.ts",
    "test:startup-record": "vitest run scripts/startup-record.test.ts",
    "check:support-row": "bun scripts/check-support-row.ts",
    "check:scene-manifest": "bun scripts/check-scene-manifest.ts",
    "test:architecture": "vitest run src/browser scripts/check-dependencies.test.ts",
    "test:browser": "playwright test",
    "check:dependencies": "bun scripts/check-dependencies.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/three": "^0.185.4",
    "happy-dom": "^20.11.6",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
