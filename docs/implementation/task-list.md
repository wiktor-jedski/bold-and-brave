# Task List

Valid Task statuses: OPEN, PREPARED, PASSED
ID: Use growing unique integers

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 1 | None — repository documentation | PASSED | Replace the obsolete feature list in `README.md` with a short description of the Playable Vertical Slice. Use the canonical terms from [CONTEXT.md](../../CONTEXT.md), link the normative [requirements](../requirements.md) and [architecture](../architecture.md), and do not add implementation details or duplicate either contract. This produces the [repository-description result](plan.md#phase-1--repository-description). |  | Manual review passes when the rendered `README.md` gives a concise Playable Vertical Slice description, uses the canonical terms, links all three normative sources, contains none of the obsolete feature-list entries, and makes no product promise outside the current slice. No automated test or command applies because this task changes repository documentation only; see the recorded testing coverage deviation. |
| 2 | ARCH-024 Browser Deployment | PASSED | Create the Bun-managed Vite, TypeScript, Vitest, and Playwright project with separate core and browser TypeScript projects. Commit `bun.lock`, keep the core compiler configuration free of DOM libraries, and make the browser project depend on the core project. This supplies the browser application required by [REQ-111](../requirements.md#req-111--authoritative-simulation-seam), follows [REQ-121](../requirements.md#req-121--platform-neutral-dependency-direction), and applies [ARCH-024](../architecture.md#arch-024--browser-deployment) and the [system overview](../architecture.md#system-overview). | 1 | From a clean checkout, `bun install --frozen-lockfile`, `bun run typecheck:core`, and `bun run build` exit 0. The core compiler uses only platform-neutral libraries, the browser build emits Vite output, and `bun.lock` makes installation reproducible. |
| 3 | ARCH-001 Simulation, ARCH-002 Simulation Interface, ARCH-003 Authoritative Simulation State | PASSED | Implement `src/core/simulation` as one core-owned module. Its small public interface exposes a factory and `readProjection`; its implementation privately owns the initial Simulation tick of 0 and returns a frozen readonly projection. Export no mutable state and no browser, DOM, Three.js, Web Audio, or IndexedDB type. This realizes the deep seam in [REQ-111](../requirements.md#req-111--authoritative-simulation-seam), the platform-neutral direction in [REQ-121](../requirements.md#req-121--platform-neutral-dependency-direction), and the contracts of [ARCH-001](../architecture.md#arch-001--simulation), [ARCH-002](../architecture.md#arch-002--simulation-interface), and [ARCH-003](../architecture.md#arch-003--authoritative-simulation-state). | 2 | `bun run test:simulation` and `bun run typecheck:core` exit 0. Vitest proves that the factory returns only the public `Simulation` interface, two reads report tick 0, `Object.isFrozen` is true, an attempted mutation fails, and a later read remains unchanged. Type assertions reject mutable projection fields and browser types in the public interface. |
| 4 | ARCH-001 Simulation, ARCH-002 Simulation Interface, ARCH-024 Browser Deployment | PASSED | Implement the browser composition root as the only production creation site for `Simulation`. The browser uses only the core-owned public interface and reads the initial projection without receiving a state mutator. Add dependency rules that reject imports from core code to browser code and reject browser imports of private Simulation implementation files. This completes the [Simulation-boundary result](plan.md#phase-2--simulation-boundary) through [ARCH-001](../architecture.md#arch-001--simulation), [ARCH-002](../architecture.md#arch-002--simulation-interface), [ARCH-024](../architecture.md#arch-024--browser-deployment), [REQ-111](../requirements.md#req-111--authoritative-simulation-seam), and [REQ-121](../requirements.md#req-121--platform-neutral-dependency-direction). | 3 | `bun run test:architecture`, `bun run check:dependencies`, `bun run test:browser`, and `bun run build` exit 0. The Vitest integration uses the real Simulation module, observes exactly one factory call, reads a frozen tick-0 projection, and confirms that a mutation attempt leaves a second read unchanged. Controlled dependency fixtures fail for a core-to-browser import and a private-implementation import. Playwright starts the built application and reports no page or console error. A review of changed files finds no mutable campaign or battle state outside `Simulation` and no unresolved finding. |
| 5 | ARCH-001 Simulation, ARCH-002 Simulation Interface, ARCH-005 Deterministic Time and Randomness, ARCH-025 Scenario Harness | PASSED | Extend the core-owned `Simulation` interface with one `advanceTick` operation and implement it as the only external way to advance the private Simulation tick. Each call advances exactly one fixed tick. A browser-independent scenario caller advances an exact requested count through the same operation, with no scenario-only state mutator. Keep `readProjection` frozen and readonly. This supplies the [planned runtime result](plan.md#phase-3--fixed-tick-runtime) required by [REQ-113](../requirements.md#req-113--fixed-tick-execution) and [PVS-ARC-003](../../.scratch/playable-vertical-slice/spec.md#authoritative-simulation-and-adapters), and follows [ARCH-001](../architecture.md#arch-001--simulation), [ARCH-002](../architecture.md#arch-002--simulation-interface), [ARCH-005](../architecture.md#arch-005--deterministic-time-and-randomness), and [ARCH-025](../architecture.md#arch-025--scenario-harness). | 4 | `bun run test:simulation` and `bun run typecheck:core` exit 0. `src/core/simulation/simulation.test.ts` proves that one public `advanceTick` call changes tick 0 to tick 1, 60 calls end at exactly tick 60, and a scenario-style caller that requests 137 ticks ends at exactly tick 137 with no extra advance. The same test proves that projections stay frozen, an attempted projection mutation cannot change the tick, and the public interface contains no browser type. |
| 6 | ARCH-005 Deterministic Time and Randomness, ARCH-006 Browser Runtime, ARCH-008 Browser Frame Collaboration | PASSED | Implement `src/browser/runtime` as a Browser Runtime module driven by an injected `requestAnimationFrame` scheduler. Accumulate callback elapsed time at one fixed interval of `1000 / 60` milliseconds, call `Simulation.advanceTick` for each due interval, process at most five due ticks per rendered frame, and retain all undispatched whole intervals and the fractional remainder for later frames. Add the focused `test:runtime` command. This implements the browser timing part of the [planned result](plan.md#phase-3--fixed-tick-runtime), [REQ-113](../requirements.md#req-113--fixed-tick-execution), and [PVS-ARC-003](../../.scratch/playable-vertical-slice/spec.md#authoritative-simulation-and-adapters) under [ARCH-005](../architecture.md#arch-005--deterministic-time-and-randomness), [ARCH-006](../architecture.md#arch-006--browser-runtime), and [ARCH-008](../architecture.md#arch-008--browser-frame-collaboration). | 5 | `bun run test:runtime` and `bun run typecheck:browser` exit 0. `src/browser/runtime/runtime.test.ts` drives controlled frame timestamps through the real public `Simulation` interface. It proves that one Simulation second of regular frame time produces exactly 60 ticks; a delayed frame that owes 12 ticks produces only 5; two later callbacks consume the retained debt as 5 and 2 even when each callback adds less than one tick; and a fractional remainder combines with later elapsed time instead of being dropped. |
| 7 | ARCH-002 Simulation Interface, ARCH-006 Browser Runtime, ARCH-008 Browser Frame Collaboration, ARCH-024 Browser Deployment | PASSED | Wire the Browser Runtime into the browser composition root and startup so one runtime schedules production `window.requestAnimationFrame` callbacks and advances the same single `Simulation` created by the composition root. Expose only the lifecycle needed by startup and tests. Do not add another Simulation, timing loop, or gameplay state. Extend the composition-root integration and browser smoke coverage. This completes the [planned result](plan.md#phase-3--fixed-tick-runtime), [REQ-113](../requirements.md#req-113--fixed-tick-execution), and [PVS-ARC-003](../../.scratch/playable-vertical-slice/spec.md#authoritative-simulation-and-adapters) through [ARCH-002](../architecture.md#arch-002--simulation-interface), [ARCH-006](../architecture.md#arch-006--browser-runtime), [ARCH-008](../architecture.md#arch-008--browser-frame-collaboration), and [ARCH-024](../architecture.md#arch-024--browser-deployment). | 6 | `bun run test:architecture`, `bun run test:runtime`, `bun run test:browser`, and `bun run build` exit 0. `src/browser/compositionRoot.test.ts` uses a controlled frame scheduler with the real Simulation, observes one Simulation factory call and one runtime frame loop, and proves that a delayed frame advances five ticks while later frames advance the retained debt on that same Simulation. `tests/e2e/boot.spec.ts` proves that the built page starts the production frame loop without a page or console error. |
| 8 | ARCH-001 Simulation, ARCH-002 Simulation Interface, ARCH-003 Authoritative Simulation State, ARCH-016 Typed Content Catalog | PASSED | Implement the initial named-Agent relationship state as plain data behind the core-owned `Simulation` interface. Add read-only typed content for Village Elder (`poc-contract-giver`) as the Contract-giver Agent and Varek (`poc-enemy-agent`) as the Enemy Agent; have each new `createSimulation` copy that content into private state; and project deep-immutable Agent records. Set Village Elder to `Active` and `Neutral`, set Varek to `Active` and `Hostile`, give both empty Grievance lists, and do not create a relationship record for Miro or a generic settlement resident. This delivers the Agent part of the [Phase 4 result](plan.md#phase-4--initial-campaign-state) required by [REQ-167](../requirements.md#req-167--initial-named-agent-identities-and-state) and [PVS-REL-001](../../.scratch/playable-vertical-slice/spec.md#initial-agent-state) under [ARCH-001](../architecture.md#arch-001--simulation), [ARCH-002](../architecture.md#arch-002--simulation-interface), [ARCH-003](../architecture.md#arch-003--authoritative-simulation-state), and [ARCH-016](../architecture.md#arch-016--typed-content-catalog). | 7 | `bun run test:simulation` and `bun run typecheck:core` exit 0. Focused tests in `src/core/simulation/simulation.test.ts` create two new Simulations and prove that each Agent projection is plain data with exactly two records and the exact IDs, names, roles, `Active` fates, `Neutral`/`Hostile` Dispositions, and empty Grievance lists. They prove that the two Agent sub-projections are value-equal, have no Miro or generic settlement-resident relationship record, are deeply frozen, do not share record or list references, and reject mutation without changing later reads. |
| 9 | ARCH-001 Simulation, ARCH-002 Simulation Interface, ARCH-003 Authoritative Simulation State, ARCH-016 Typed Content Catalog | PASSED | Extend the same initial campaign state and plain projection with the player character, Miro (`poc-companion`) as the fixed Companion, and the initial resources. Add read-only typed content for the player-character and Miro identities and Miro's 0-Coin cost; initialize Band membership with both identities; set Coin to 100 and Provisions to 10.0; and do not apply a Coin deduction when Miro joins the new campaign. Keep these values private to the Simulation and expose only deep-immutable projection data. This completes the [Phase 4 result](plan.md#phase-4--initial-campaign-state) required by [REQ-077](../requirements.md#req-077--initial-preparation-resources) and [PVS-PRP-001](../../.scratch/playable-vertical-slice/spec.md#6-preparation-coin-and-provisions) under [ARCH-001](../architecture.md#arch-001--simulation), [ARCH-002](../architecture.md#arch-002--simulation-interface), [ARCH-003](../architecture.md#arch-003--authoritative-simulation-state), and [ARCH-016](../architecture.md#arch-016--typed-content-catalog). | 8 | `bun run test:simulation`, `bun run typecheck:core`, and `scripts/ci-check.py` exit 0. A focused test in `src/core/simulation/simulation.test.ts` creates two new Simulations and compares each complete tick-0 plain-state projection with one exact expected value containing the Agent state from task 8, 100 Coin, 10.0 Provisions, and Band membership of the player character and Miro. It proves that Miro has ID `poc-companion` and is present while Coin remains 100, the two complete projections are value-equal but have separate deep-immutable nested data, and repeated reads or `advanceTick` do not alter the initial Agent, Band, Coin, or Provisions values. |
| 10 | ARCH-024 Browser Deployment | PASSED | Add one deeply immutable support-promise record as the only authored support row. Export it as read-only Browser Deployment data that the product surface and promised-row checks share. The record contains the exact Chromium version, Linux x64 without a distribution version, the NVIDIA GPU and driver, the CSS-pixel viewport, the maximum device-pixel ratio, and normal keyboard-and-mouse input as one combined mode. Add the focused `test:support` command. This starts the [Phase 5 result](plan.md#phase-5--support-promise), implements [REQ-012](../requirements.md#req-012--promised-machine), [REQ-013](../requirements.md#req-013--promised-row-test-display), and the support-envelope part of [REQ-015](../requirements.md#req-015--input-and-support-exclusions), follows [PVS-SCP-007, PVS-SCP-008, and PVS-SCP-010](../../.scratch/playable-vertical-slice/spec.md#scope-contract), and applies [ARCH-024](../architecture.md#arch-024--browser-deployment). | 9 | `bun run test:support` and `bun run typecheck:browser` exit 0. A focused test in `src/browser/support/supportPromise.test.ts` reads the public record and proves that it is deeply frozen and has exactly one row with Chromium 151.0.7922.137, Linux x64, NVIDIA RTX 2070 SUPER, driver 610.57.04, a 1920 × 1080 CSS-pixel viewport, maximum device-pixel ratio 1.0, and one normal keyboard-and-mouse mode. The exact-value assertion fails for an added row, Linux distribution field, alternate browser, alternate input mode, GPU, or driver. |
| 11 | ARCH-024 Browser Deployment | PASSED | Present the shared support-promise record in the built browser product as one semantic support table with exactly one body row. Show the browser, operating-system architecture, GPU, driver, viewport, device-pixel ratio, and combined keyboard-and-mouse mode from the record; do not copy the values into a second product statement. Keep the application name and the one production frame loop. This makes the [Phase 5 result](plan.md#phase-5--support-promise) observable, completes the authored-promise part of [REQ-012](../requirements.md#req-012--promised-machine) and [REQ-015](../requirements.md#req-015--input-and-support-exclusions), follows [PVS-SCP-007 and PVS-SCP-010](../../.scratch/playable-vertical-slice/spec.md#scope-contract), and keeps the surface in [ARCH-024](../architecture.md#arch-024--browser-deployment). | 10 | `bun run test:support`, `bun run test:browser`, and `bun run build` exit 0. `tests/e2e/support.spec.ts` opens the built product, finds one support table and one body row, and compares every cell with the shared record. The rendered row names Chromium 151.0.7922.137, Linux x64, NVIDIA RTX 2070 SUPER, driver 610.57.04, 1920 × 1080 CSS pixels, maximum device-pixel ratio 1.0, and normal keyboard-and-mouse input. The support surface contains no Linux distribution version or promise for keyboard-only, touch, mobile, reduced motion, another browser, another GPU, or another driver. The existing browser smoke check still proves that the production frame loop runs without page or console errors. |
| 12 | ARCH-024 Browser Deployment, ARCH-028 Performance and Resource Limits | PASSED | Add a dedicated local promised-row acceptance command and Playwright configuration that reuse the shared support record. Resolve the system Chromium executable from `PATH`, reject an environment that does not match the promised browser, Linux x64 architecture, GPU, or driver, and set the promised viewport and device-pixel ratio. Record the actual browser version, platform, architecture, GPU, driver, browser dimensions, and device-pixel ratio as machine-readable test output without recording a Linux distribution version. Keep GitHub-hosted pull-request CI on the existing general Playwright browser check and do not treat it as promised-row evidence. This completes the focused [Phase 5 check](plan.md#phase-5--support-promise) for [REQ-012](../requirements.md#req-012--promised-machine) and [REQ-013](../requirements.md#req-013--promised-row-test-display), records the support-promise contribution to [REQ-015](../requirements.md#req-015--input-and-support-exclusions), whose usability evidence remains assigned to [Phase 38](plan.md#phase-38--visual-and-interface-contract), follows the focused-check rule for [CP-SUPPORT-GATE and CP-PERFORMANCE](../requirements.md#acceptance-checkpoint-catalog), and applies [ARCH-024](../architecture.md#arch-024--browser-deployment) and [ARCH-028](../architecture.md#arch-028--performance-and-resource-limits). | 11 | `bun run test:support` and `scripts/ci-check.py` exit 0 in general CI. On the promised workstation, `bun run check:support-row` exits 0. Mismatch tests reject each wrong browser version, platform, architecture, GPU, driver, viewport, and device-pixel ratio before they can produce passing evidence. The local promised-row command launches the system Chromium, writes `test-results/support-row/environment.json`, and the record contains Chromium 151.0.7922.137, Linux x64, NVIDIA RTX 2070 SUPER, driver 610.57.04, a 1920 × 1080 CSS-pixel browser viewport, and device-pixel ratio 1.0. Review finds one product support row, no Linux distribution field, no alternate browser, input, GPU, or driver promise, no promised-row claim in the GitHub-hosted workflow, no unresolved finding, and no regression in checks from complete phases. |
| 13 | ARCH-006 Browser Runtime, ARCH-023 Browser Delivery State, ARCH-024 Browser Deployment | PASSED | Implement `src/browser/startup` as the ordered Browser Delivery State capability gate. Check the secure context and `navigator.gpu`, request one adapter with `high-performance` only as `powerPreference`, and accept only a nonnull adapter whose `adapter.info.isFallbackAdapter` value is exactly `false`. Record the selected adapter information and every reported limit before device use, then request one usable device with an empty descriptor so no optional feature or raised limit is required. Do not use the power preference or a product-side vendor allowlist as proof of a physical adapter. Return one typed, readable `Unsupported` result at the failed check, or the selected adapter, device, and immutable inspection record for the Three.js backend check. Add the focused `test:startup` command. This starts the [Phase 6 result](plan.md#phase-6--webgpu-startup), implements [REQ-014](../requirements.md#req-014--webgpu-startup-gates), the startup portion of [REQ-134](../requirements.md#req-134--ordered-browser-delivery-states), and [REQ-135](../requirements.md#req-135--core-webgpu-request), follows [PVS-SCP-009](../../.scratch/playable-vertical-slice/spec.md#scope-contract) and [PVS-WEB-001 and PVS-WEB-002](../../.scratch/playable-vertical-slice/spec.md#webgpu-startup-loading-and-device-state), and applies [ARCH-006](../architecture.md#arch-006--browser-runtime), [ARCH-023](../architecture.md#arch-023--browser-delivery-state), and [ARCH-024](../architecture.md#arch-024--browser-deployment). | 12 | `bun run test:startup` and `bun run typecheck:browser` exit 0. `src/browser/startup/startupGate.test.ts` records every capability operation and proves the exact secure-context, WebGPU-presence, adapter, and device order. Separate cases for an insecure context, absent `navigator.gpu`, null adapter, `isFallbackAdapter: true`, absent fallback information, and failed device request return their specified `Unsupported` code and readable message and perform no later operation. The success case proves that `requestAdapter` receives only `powerPreference: "high-performance"`, accepts the qualifying nonfallback adapter returned by the browser without a power-class rejection, reads adapter information and every reported limit before `requestDevice`, passes an empty device descriptor, and enables no optional adapter feature or raised limit. |
| 14 | ARCH-009 Three.js Presentation Adapter, ARCH-023 Browser Delivery State, ARCH-024 Browser Deployment | PASSED | Add the Bun-locked Three.js dependency and implement the WebGPU backend gate in the Three.js Presentation Adapter. Initialize `WebGPURenderer` with the exact device selected by the capability gate, wait for initialization, and accept the renderer only when its initialized backend identifies itself as WebGPU. A renderer initialization failure or a non-WebGPU backend returns the governed readable WebGPU-backend `Unsupported` result, disposes the rejected renderer, and exposes no WebGL fallback path. Add the focused `test:presentation` command. This supplies the renderer part of the [Phase 6 result](plan.md#phase-6--webgpu-startup), implements [REQ-011](../requirements.md#req-011--webgpu-only-rendering) and the backend transition in [REQ-134](../requirements.md#req-134--ordered-browser-delivery-states), preserves the selected-device contract of [REQ-135](../requirements.md#req-135--core-webgpu-request), follows [PVS-SCP-006](../../.scratch/playable-vertical-slice/spec.md#scope-contract) and [PVS-WEB-001](../../.scratch/playable-vertical-slice/spec.md#webgpu-startup-loading-and-device-state), and applies [ARCH-009](../architecture.md#arch-009--threejs-presentation-adapter), [ARCH-023](../architecture.md#arch-023--browser-delivery-state), and [ARCH-024](../architecture.md#arch-024--browser-deployment). | 13 | `bun run test:presentation`, `bun run typecheck:browser`, and `bun run build` exit 0. `src/browser/presentation/webgpuBackend.test.ts` uses an injected renderer factory and proves that initialization receives the capability gate's device once, completes before backend inspection, and returns success only for a WebGPU backend. Separate initialization-error and forced non-WebGPU-backend cases return the specified readable failure, dispose the renderer, and never call a render or Scene-loading operation. The production build imports the Three.js WebGPU renderer and contains no successful WebGL fallback branch. |
| 15 | ARCH-006 Browser Runtime, ARCH-009 Three.js Presentation Adapter, ARCH-010 DOM Interface Adapter, ARCH-023 Browser Delivery State, ARCH-024 Browser Deployment | PASSED | Wire the capability gate and Three.js backend gate into the browser composition root and startup surface. Keep the delivery state at `Startup` while checks run, show one specific semantic DOM `Unsupported` alert for the first failed check, and enter `Loading Scene` only after every check passes. Start the one Browser Runtime and invoke the Scene-loading handoff only on success; a failure starts neither and cannot run a later gate. Preserve the application name, the single support-promise table, the single Simulation, and the single frame loop. This makes the [Phase 6 result](plan.md#phase-6--webgpu-startup) observable for [REQ-011](../requirements.md#req-011--webgpu-only-rendering), [REQ-014](../requirements.md#req-014--webgpu-startup-gates), [REQ-134](../requirements.md#req-134--ordered-browser-delivery-states), and [REQ-135](../requirements.md#req-135--core-webgpu-request), follows the ordered states in [PVS-WEB-001](../../.scratch/playable-vertical-slice/spec.md#webgpu-startup-loading-and-device-state) and the error-surface rule in [PVS-UI-009](../../.scratch/playable-vertical-slice/spec.md#visual-and-interface-contract), and applies [ARCH-006](../architecture.md#arch-006--browser-runtime), [ARCH-009](../architecture.md#arch-009--threejs-presentation-adapter), [ARCH-010](../architecture.md#arch-010--dom-interface-adapter), [ARCH-023](../architecture.md#arch-023--browser-delivery-state), and [ARCH-024](../architecture.md#arch-024--browser-deployment). | 13, 14 | `bun run test:startup`, `bun run test:presentation`, `bun run test:architecture`, `bun run test:browser`, and `bun run build` exit 0. Composition-root integration tests inject controlled capability, renderer, scheduler, and Scene-loading collaborators and prove the full ordered success trace. Each secure-context, WebGPU-unavailable, physical-adapter, device-initialization, and backend failure stops at its first failing operation, leaves the runtime stopped, invokes no Scene-loading handoff, and renders its exact readable alert. `tests/e2e/boot.spec.ts` forces an insecure built-product startup, observes the secure-context alert, no Scene asset request, and no production frame request. A focused surface test proves that the successful state is `Loading Scene` and that the application still has one name and one support table. |
| 16 | ARCH-006 Browser Runtime, ARCH-009 Three.js Presentation Adapter, ARCH-023 Browser Delivery State, ARCH-024 Browser Deployment | PASSED | Extend the local promised-row acceptance command to exercise the real Phase 6 startup through the built product. Launch the system Chromium in headed mode through the active desktop session, remove Playwright's unsafe SwiftShader default, and add no flag that enables WebGPU or bypasses the GPU blocklist. Reuse the verified support row, require exactly one matching host GPU and driver row, wait for `Loading Scene`, and write a machine-readable startup record only after the product reports the ordered secure-context, physical-adapter, core-device, and Three.js WebGPU-backend successes. Correlate the browser adapter vendor and nonfallback signal with the exact host GPU, and record its full available information and tested limits, the empty core-only device request, selected backend, final delivery state, and absence of a WebGL fallback. Keep the general pull-request browser check free of a promised-row claim. This completes the focused [Phase 6 check](plan.md#phase-6--webgpu-startup) for [REQ-011](../requirements.md#req-011--webgpu-only-rendering), [REQ-014](../requirements.md#req-014--webgpu-startup-gates), [REQ-134](../requirements.md#req-134--ordered-browser-delivery-states), and [REQ-135](../requirements.md#req-135--core-webgpu-request), exercises [SCN-15-WEBGPU-STARTUP-LOAD and CP-SUPPORT-GATE](../../.scratch/playable-vertical-slice/spec.md#checkpoint-traceability-matrix) at the current phase boundary, and applies [ARCH-006](../architecture.md#arch-006--browser-runtime), [ARCH-009](../architecture.md#arch-009--threejs-presentation-adapter), [ARCH-023](../architecture.md#arch-023--browser-delivery-state), and [ARCH-024](../architecture.md#arch-024--browser-deployment). | 15 | `scripts/ci-check.py` exits 0 in general CI. On the promised workstation in an active graphical session, `bun run check:support-row` exits 0 and `tests/e2e-support-row/support-row.spec.ts` observes the built product in `Loading Scene` after a real headed startup. The command writes `test-results/support-row/startup.json` only after validation; the record shows a secure context, the exact gate order, `powerPreference: "high-performance"`, exactly one host NVIDIA RTX 2070 SUPER with driver 610.57.04, a browser adapter with NVIDIA vendor information and `isFallbackAdapter: false`, adapter information and every reported limit inspected before device use, an empty device descriptor with no enabled optional adapter feature or raised limit, one usable device, a Three.js WebGPU backend, and no WebGL fallback. A headless launch, unsafe WebGPU enablement or blocklist-bypass flag, software adapter, failed gate, or mismatched record makes the command nonzero and leaves no passing startup record. Review finds no Scene asset request before all gates pass and no promised-row claim in the GitHub-hosted workflow. |
| 17 | ARCH-009 Three.js Presentation Adapter, ARCH-010 DOM Interface Adapter, ARCH-016 Typed Content Catalog, ARCH-022 Scene Transition and Loading Collaboration, ARCH-023 Browser Delivery State, ARCH-024 Browser Deployment | PREPARED | Deliver one successful startup Scene load through the built product. Add one deeply immutable startup manifest to `src/core/content` for Scene `poc-overworld` and glTF asset `poc-overworld-environment`, with one small authored asset that contains nodes for the initial Band and one animation clip. After all startup gates pass, use the initialized `WebGPURenderer` to fetch the asset as a stream, show download progress, decode it with `GLTFLoader`, attach it to one Three.js Scene, create one third-person `PerspectiveCamera` and the required `AnimationMixer`, prepare GPU resources, render one frame, and enter `Ready`. Set no elapsed-time limit. Add the renderer canvas to the existing product surface and keep one renderer, device, Simulation, and frame loop. Add `scripts/check-scene-manifest.ts` as the `check:scene-manifest` content-contract integration command, and make `scripts/ci-check.py` run it. The command must read the real public content catalog, resolve the same authored asset that the production loader receives, and use no injected or recording test collaborator. Extend dependency rules to reject browser or Three.js imports from the core manifest. This starts the [planned result](plan.md#phase-7--scene-loading-and-renderer-boundary), implements the successful path of [REQ-136](../requirements.md#req-136--scene-asset-loading-progress), follows [PVS-WEB-001 and PVS-WEB-003](../../.scratch/playable-vertical-slice/spec.md#webgpu-startup-loading-and-device-state), and applies [ARCH-009](../architecture.md#arch-009--threejs-presentation-adapter), [ARCH-010](../architecture.md#arch-010--dom-interface-adapter), [ARCH-016](../architecture.md#arch-016--typed-content-catalog), [ARCH-022](../architecture.md#arch-022--scene-transition-and-loading-collaboration), [ARCH-023](../architecture.md#arch-023--browser-delivery-state), and [ARCH-024](../architecture.md#arch-024--browser-deployment). | 16 | `scripts/ci-check.py` exits 0 and its output includes `bun run check:scene-manifest`. That command loads the actual startup manifest through the public core catalog and fails unless the manifest and every nested record and list are frozen, `poc-overworld` contains `poc-overworld-environment`, all Scene and asset IDs are unique, and the production asset reference resolves to the committed authored glTF file. `bun run check:dependencies` fails for a browser or Three.js import in the core manifest. On the promised workstation in an active graphical session, `bun run check:support-row` exits 0 and `tests/e2e-support-row/support-row.spec.ts` drives the real headed built product from the startup gates through one asset request to `Ready`. The product visibly reports download, decode, GPU upload, and Scene readiness in order. A Playwright-delayed asset response completes without a load timeout. The check observes one WebGPU canvas, the authored animation, and no WebGL path or second frame loop. The command records the exact Scene ID, asset ID, stage order, initialized WebGPU backend, and final `Ready` state only after the real load passes. Review finds no load deadline, timer, second renderer, second device, or second Simulation. |
| 18 | ARCH-002 Simulation Interface, ARCH-006 Browser Runtime, ARCH-008 Browser Frame Collaboration, ARCH-009 Three.js Presentation Adapter, ARCH-012 Presentation Collaboration | OPEN | Present current read-only Simulation output through the loaded Three.js WebGPU Scene. Extend the Browser Runtime so that, after each fixed-tick batch, it reads one immutable projection and calls the Three.js presenter once from the same frame loop. Pass only the projection and interpolation timing. Use projected Band IDs to update presentation-only node visibility, interpolation, and animation feedback. The adapter can own Three.js objects and interpolation history, but it must not store a projection, resource value, combat result, relationship result, fate result, or outcome. Missing presentation output has no write path to the Simulation. This completes the renderer boundary in the [planned result](plan.md#phase-7--scene-loading-and-renderer-boundary), implements [REQ-118](../requirements.md#req-118--threejs-presentation-role), follows [PVS-ARC-008](../../.scratch/playable-vertical-slice/spec.md#authoritative-simulation-and-adapters), and applies [ARCH-002](../architecture.md#arch-002--simulation-interface), [ARCH-006](../architecture.md#arch-006--browser-runtime), [ARCH-008](../architecture.md#arch-008--browser-frame-collaboration), [ARCH-009](../architecture.md#arch-009--threejs-presentation-adapter), and [ARCH-012](../architecture.md#arch-012--presentation-collaboration). | 17 | `scripts/ci-check.py` exits 0. On the promised workstation in an active graphical session, `bun run check:support-row` exits 0 and the real headed built product reaches `Ready`, renders the two projected initial Band members through Three.js WebGPU, and advances its authored animation from the current projection tick and interpolation value on the existing frame loop. `bun run test:architecture` and `bun run check:dependencies` exit 0 and prove that the presenter receives the public immutable `SimulationProjection`, imports no private Simulation implementation, and has no authoritative-state write dependency. Review of the runtime operation order confirms all due ticks, one projection read, one presentation call, and one next-frame request. Review of adapter-owned state finds only Three.js objects, load state, and interpolation history. These checks exercise [CP-ARCH-DETERMINISM](../../.scratch/playable-vertical-slice/spec.md#checkpoint-traceability-matrix) at this implementation boundary. |
| 19 | ARCH-009 Three.js Presentation Adapter, ARCH-010 DOM Interface Adapter, ARCH-022 Scene Transition and Loading Collaboration, ARCH-023 Browser Delivery State, ARCH-024 Browser Deployment | OPEN | Add complete Scene-load diagnostics, first-error stop, and Retry behavior to the production load. Write structured browser-console records for the Scene load, each asset download and decode, GPU upload, every progress update, completion, and failure. Put `poc-overworld` and `poc-overworld-environment` in every applicable record. The first failed stage enters `Load failed`, runs no later stage, shows its readable error and one semantic Retry action, and starts no automatic retry. Retry uses the same initialized WebGPU renderer, returns to `Loading Scene`, and restarts at download. Full success enters `Ready`. This completes the [planned result](plan.md#phase-7--scene-loading-and-renderer-boundary), implements [REQ-137](../requirements.md#req-137--scene-load-diagnostics) and the failure and Retry parts of [REQ-136](../requirements.md#req-136--scene-asset-loading-progress), preserves [REQ-134](../requirements.md#req-134--ordered-browser-delivery-states), follows [PVS-WEB-001, PVS-WEB-003, and PVS-WEB-004](../../.scratch/playable-vertical-slice/spec.md#webgpu-startup-loading-and-device-state) and [PVS-UI-009](../../.scratch/playable-vertical-slice/spec.md#visual-and-interface-contract), and applies [ARCH-009](../architecture.md#arch-009--threejs-presentation-adapter), [ARCH-010](../architecture.md#arch-010--dom-interface-adapter), [ARCH-022](../architecture.md#arch-022--scene-transition-and-loading-collaboration), [ARCH-023](../architecture.md#arch-023--browser-delivery-state), and [ARCH-024](../architecture.md#arch-024--browser-deployment). | 18 | `scripts/ci-check.py` exits 0. On the promised workstation in an active graphical session, `bun run check:support-row` exits 0. `tests/e2e-support-row/support-row.spec.ts` runs the real headed built product once for success and once with Playwright failing the first real request for `poc-overworld-environment` after all startup gates pass. The successful run reaches `Ready` only after all four visible progress stages and the required console records. The failed run makes one asset request before `Load failed`, records the first error with both identifiers, runs no later stage, makes no request during the no-retry observation period, and keeps Retry visible. One Retry makes one new request, restarts visible progress at download, and reaches `Ready`. The command writes `test-results/support-row/scene-load.json` only after it validates the exact event order, identifiers, first-error stop, one explicit retry, WebGPU backend, and final state. A missing stage, missing identifier, automatic retry, extra asset request, WebGL backend, or invalid record makes the command nonzero and leaves no passing Scene-load record. This exercises [SCN-15-WEBGPU-STARTUP-LOAD and CP-SUPPORT-LOAD](../../.scratch/playable-vertical-slice/spec.md#checkpoint-traceability-matrix). |
